// Purpose: Configuration Tailwind CSS; definit les options globales de theming utilitaire.
// Exports: objet de config Tailwind (`darkMode: 'class'`) valide via `satisfies Config`.
import type { Config } from 'tailwindcss'

export default {
  darkMode: 'class',
} satisfies Config
